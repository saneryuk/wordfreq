C implementation of word frequency counter in a text file
Datastructure used: Linked List
Linear search when checking if the word exists
Program is not case sensitive. So for example 'when' and 'When' are counted as different words
Driver code runs as follows:
	1. File is opened using open() system call and read character by character using a while loop on the read() system call.
	2.
